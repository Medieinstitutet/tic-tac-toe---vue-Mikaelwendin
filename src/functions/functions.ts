import { GameToken } from "../models.ts/gameClass"

export const checkWin = (gameBoard:GameToken[]) => {
    
    //Kolla om någon vinner. 
}
export const swapPlayer = (player: boolean) => {
player = !player
}